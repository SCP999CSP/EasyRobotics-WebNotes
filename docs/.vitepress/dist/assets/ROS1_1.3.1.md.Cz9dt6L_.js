import{_ as a,c as i,o as n,af as t}from"./chunks/framework.DSuiHQgX.js";const m=JSON.parse('{"title":"1.3.1 HelloWorld Implementation Overview","description":"","frontmatter":{},"headers":[],"relativePath":"ROS1/1.3.1.md","filePath":"ROS1/1.3.1.md"}'),s={name:"ROS1/1.3.1.md"};function o(r,e,c,l,d,p){return n(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="_1-3-1-helloworld-implementation-overview" tabindex="-1">1.3.1 HelloWorld Implementation Overview <a class="header-anchor" href="#_1-3-1-helloworld-implementation-overview" aria-label="Permalink to “1.3.1 HelloWorld Implementation Overview”">​</a></h1><p>In ROS, the main programming languages used are C++ and Python. Most programs in ROS can be implemented in either language. In this tutorial series, each example will be demonstrated using both C++ and Python. You can choose the implementation that best suits your needs.</p><p>Even when using different programming languages, the implementation process for programs in ROS is generally similar. Taking the current HelloWorld program as an example, the implementation process is roughly as follows:</p><ol><li>First, create a workspace.</li><li>Then, create a functional package.</li><li>Edit the source files.</li><li>Edit the configuration files.</li><li>Compile and execute.</li></ol><p>In the above process, the differences between C++ and Python lie mainly in the implementation details of steps 3 and 4, while the other steps are largely the same. This section will first cover the common steps 1 and 2 for both C++ and Python programming. Sections 1.3.2 and 1.3.3 will then demonstrate how to write HelloWorld using C++ and Python, respectively.</p><h4 id="_1-create-and-initialize-a-workspace" tabindex="-1">1. Create and Initialize a Workspace <a class="header-anchor" href="#_1-create-and-initialize-a-workspace" aria-label="Permalink to “1. Create and Initialize a Workspace”">​</a></h4><p>text</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>mkdir -p custom_workspace_name/src</span></span>
<span class="line"><span>cd custom_workspace_name</span></span>
<span class="line"><span>catkin_make</span></span></code></pre></div><p>The above commands first create a workspace and a <code>src</code> subdirectory, then navigate into the workspace and compile it using the <code>catkin_make</code> command.</p><h4 id="_2-navigate-to-src-create-a-ros-package-and-add-dependencies" tabindex="-1">2. Navigate to <code>src</code>, Create a ROS Package, and Add Dependencies <a class="header-anchor" href="#_2-navigate-to-src-create-a-ros-package-and-add-dependencies" aria-label="Permalink to “2. Navigate to src, Create a ROS Package, and Add Dependencies”">​</a></h4><p>text</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>cd src</span></span>
<span class="line"><span>catkin_create_pkg custom_ROS_package_name roscpp rospy std_msgs</span></span></code></pre></div><p>The above command will generate a functional package in the workspace. This package depends on <code>roscpp</code>, <code>rospy</code>, and <code>std_msgs</code>. Here, <code>roscpp</code> is a library implemented in C++, <code>rospy</code> is implemented in Python, and <code>std_msgs</code> is the standard message library. When creating a ROS functional package, these three libraries are generally included as dependencies.</p><hr><p><strong>Note:</strong> In ROS, while C++ and Python can be used interchangeably for implementing the same functionality, the choice of language depends on the specific requirements. This is because the two languages have complementary characteristics: C++ offers high runtime efficiency but lower coding efficiency, whereas Python is the opposite. Based on these complementary traits, ROS designers developed the <code>roscpp</code> and <code>rospy</code> libraries. The former is intended to be a high-performance library for ROS, while the latter is typically used in scenarios where performance is not critical, aiming to improve development efficiency.</p>`,15)])])}const g=a(s,[["render",o]]);export{m as __pageData,g as default};
